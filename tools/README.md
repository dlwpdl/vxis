# `tools/` — Local Dev Tools

Repo-local scripts and dev utilities. Not shipped as part of the `vxis` library — these are for maintainers only.

Not to be confused with `src/vxis/agent/tools/` which contains the runtime BrainTool implementations used by the Phase A scan loop.
