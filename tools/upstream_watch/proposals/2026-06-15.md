# Upstream Watch Proposals — 2026-06-15
_16 proposals from upstream sync analysis (strix/pentagi/pentest-ai-agents/pentestagent), 2026-06-15_

Generated from the 26-agent upstream-sync workflow (adopt/defer/reject/already-have verdicts pre-filled).
Edit `Status`: `approved` / `rejected` / `deferred` / `implemented`.

---

## [1] Dedicated NoSQL injection skill (JSON-body/operator delivery + auth-bypass differential) !!!

- **ID:** `2026-06-15-R1-nosql-skill`
- **Source:** usestrix/strix — strix nosql_injection.md (#404)
- **Category:** skill-coverage
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
New frozen test_nosql skill: deliver $ne/$gt/$regex/$where as JSON-body operator objects AND bracket-notation params; oracle = auth-bypass differential + Mongo/CastError fingerprint. Wire _skill_to_vectors.

**Workflow verdict:** adopt (adjusted priority 2).

### Verify (adversarial)
VERIFIED missing: injection.json has 2 detect=[] query-STRING payloads only; $ne never reaches parser. WEB-NOSQL-001/002 vectors already exist but have NO producer (orphaned-vector penalty). Adopt. Caveat: target files must add skills/__init__ SKILL_REGISTRY + scan_loop dispatch wiring; effort M-leaning.

### Affected VXIS Files
`src/vxis/agent/skills/test_nosql.py`, `src/vxis/data/payloads/nosql.json`, `src/vxis/agent/skills/_payload_loader.py`, `src/vxis/pipeline/scan_pipeline_v2.py`, `src/vxis/agent/skills/__init__.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED missing: injection.json has 2 detect=[] query-STRING payloads only; $ne never reaches parser. WEB-NOSQL-001/002 vectors already exist but have NO producer (orphaned-vector penalty). Adopt. Caveat: target files must add skills/__ini

---

## [2] Out-of-band/OAST collaborator primitive (DNS/HTTP canary + source-IP FP oracle) !!!

- **ID:** `2026-06-15-R2-oast-collaborator`
- **Source:** usestrix/strix — strix ssrf.md source-IP FP; pentest-ai-agents poc-validator canary
- **Category:** infra-primitive
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
Shared OOB collaborator: canary domain + DNS/HTTP callback listener issuing per-probe tokens + poll API any skill consults. Add Strix source-IP FP rule (callback src IP == tester => client, not backend).

**Workflow verdict:** adopt (adjusted priority 3).

### Verify (adversarial)
VERIFIED zero OOB infra in tree; sqli_oob payload is structurally dead (.test.invalid, detect=[]); blind SSRF has no CONFIRMED path and size-only finding is exactly what verifier demotes. Highest-leverage unlock (blind SSRF/SQLi/XXE/RCE/CRLF). Caveat: needs operator infra + edits to ADR-006-frozen skills; effort L.

### Affected VXIS Files
`src/vxis/interaction/`, `src/vxis/agent/skills/test_ssrf.py`, `src/vxis/agent/skills/test_injection.py`, `src/vxis/agent/tools/verifier_tools.py`, `src/vxis/data/payloads/injection.json`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED zero OOB infra in tree; sqli_oob payload is structurally dead (.test.invalid, detect=[]); blind SSRF has no CONFIRMED path and size-only finding is exactly what verifier demotes. Highest-leverage unlock (blind SSRF/SQLi/XXE/RCE/CRL

---

## [3] Verifier gate ALL severities + single _findings.append chokepoint + verdict write-back !!!

- **ID:** `2026-06-15-R3-verifier-all-severities`
- **Source:** 0xSteph/pentest-ai-agents — pentest-ai poc-validator; ADR-012 unimplemented (a)+(b)
- **Category:** fp-verifier
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
Move verify gate to single findings-append chokepoint; apply to every reportable severity (cheap deterministic preflight always, LLM refuter on borderline). Write verdict onto persisted Finding.

**Workflow verdict:** adopt (adjusted priority 3).

### Verify (adversarial)
VERIFIED: both gate sites hard-code {high,critical}; _findings.append:883 unguarded; verdict never persisted. This is ADR-012's own stated-but-unimplemented decision (Accepted/active 2026-06-02). Sequence chokepoint+writeback first. Effort M-leaning-L (22 call sites + drift to reconcile).

### Affected VXIS Files
`src/vxis/agent/tools/finding_tools.py`, `src/vxis/agent/scan_loop_run.py`, `src/vxis/agent/scan_loop_actions.py`, `src/vxis/agent/tools/verifier_tools.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: both gate sites hard-code {high,critical}; _findings.append:883 unguarded; verdict never persisted. This is ADR-012's own stated-but-unimplemented decision (Accepted/active 2026-06-02). Sequence chokepoint+writeback first. Effort 

---

## [4] Mid-scan checkpoint/resume of the ReAct loop (atomic snapshot) !!!

- **ID:** `2026-06-15-R4-react-loop-resume`
- **Source:** usestrix/strix — strix implicit crash-resume + 9-gap resume audit
- **Category:** persistence-resume
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
Add to_snapshot/from_snapshot to ScanLoopState persisted atomically (tempfile+os.replace); honor resume_from; implicit resume by run-dir presence.

**Workflow verdict:** adopt (adjusted priority 6).

### Verify (adversarial)
VERIFIED: ScanLoopState has no serialize/restore; save/load_checkpoint dead code; resume_from explicitly ignored (scan_pipeline_v2.py:719). DROP the 'corrupt-findings-zeroing' framing (findings are in-memory, no disk file to corrupt). Effort L-to-XL (HypothesisDAG + 2 deques have no serialization).

### Affected VXIS Files
`src/vxis/agent/scan_loop_state.py`, `src/vxis/pipeline/scan_pipeline_v2.py`, `src/vxis/pipeline/context.py`, `src/vxis/cli/main.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: ScanLoopState has no serialize/restore; save/load_checkpoint dead code; resume_from explicitly ignored (scan_pipeline_v2.py:719). DROP the 'corrupt-findings-zeroing' framing (findings are in-memory, no disk file to corrupt). Effor

---

## [5] Per-decision-class reasoning_effort on OpenAI payload (RESCOPED to S) !!!

- **ID:** `2026-06-15-R5-reasoning-effort-gate`
- **Source:** usestrix/strix + vxcontrol/pentagi — strix reasoning-effort registry gate; pentagi per-role thinking
- **Category:** llm-ops
- **Priority(impact):** high
- **Overlap:** partial-overlap (50%)

### Description
Send reasoning_effort high/xhigh for exploit/chain/verify, low/none for recon/triage JSON calls. (Registry gate + tool_choice drop + per-role thinking ALREADY EXIST.)

**Workflow verdict:** defer (adjusted priority 3).

### Verify (adversarial)
Partial-adopt. Genuine kernel real: reasoning_effort never sent (0 grep hits). BUT 3/4 deliverables already exist (tool_choice drop, registry prefix-strip gate, per-role thinking extra_body); the 'CoT leak corrupts severity oracle' rationale is FALSE (oracle is pure regex); file paths wrong (llm/ not agent/). Rescope to ~S: just add effort to payload + decision-class map.

### Affected VXIS Files
`src/vxis/agent/brain.py`, `src/vxis/llm/hybrid_config.py`, `src/vxis/agent/scan_loop_v3.py`

### Decision
- **Status:** `deferred`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=defer; Partial-adopt. Genuine kernel real: reasoning_effort never sent (0 grep hits). BUT 3/4 deliverables already exist (tool_choice drop, registry prefix-strip gate, per-role thinking extra_body); the 'CoT leak corrupts severity oracle' rational

---

## [6] Deterministic empty-array / identical-content IDOR negative oracle !!!

- **ID:** `2026-06-15-R6-idor-empty-array-oracle`
- **Source:** usestrix/strix — strix idor.md empty-array FP signal
- **Category:** fp-verifier
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
Before flagging IDOR: reject empty array/object, generic not-found JSON, or identical content across all probed IDs; require unauthorized body to differ from owner baseline.

**Workflow verdict:** adopt (adjusted priority 7).

### Verify (adversarial)
VERIFIED: test_idor.py:209/275 flag on status==200 & >50B or count alone, no empty/identical/baseline-diff check. Cheap deterministic FP fix the LLM verifier can't reliably catch. Effort S. DROP the cross-channel (REST+GraphQL) booster (not cheap, doesn't exist). Fix belongs in skill oracle, not verifier_tools.

### Affected VXIS Files
`src/vxis/agent/skills/test_idor.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: test_idor.py:209/275 flag on status==200 & >50B or count alone, no empty/identical/baseline-diff check. Cheap deterministic FP fix the LLM verifier can't reliably catch. Effort S. DROP the cross-channel (REST+GraphQL) booster (not

---

## [7] Standardize per-skill Validation + False-Positives negative-oracle doctrine (data-driven) !!

- **ID:** `2026-06-15-R7-skill-fp-schema`
- **Source:** usestrix/strix + GH05TCREW/pentestagent — strix skill markdown schema; pentestagent anti-hallucination
- **Category:** fp-verifier
- **Priority(impact):** medium
- **Overlap:** partial-overlap (50%)

### Description
Extend doctrine dataset (validation + false_positives fields) to the 11 skills lacking it; close half-wired gap where verifier _doctrine_flags keys on phrases absent from shipped doctrine JSON.

**Workflow verdict:** defer (adjusted priority 4).

### Verify (adversarial)
Substantially ALREADY-HAVE: doctrine-as-data + validation field + verifier consumption + 'no finding without control' rule all exist (injection/xss/ssrf). Residual = data-fill for 11 skills + add false_positives field + rewire _doctrine_flags. Defer behind higher-leverage work; the FP-critical surfaces are already covered.

### Affected VXIS Files
`src/vxis/data/payloads/`, `src/vxis/agent/tools/verifier_tools.py`, `src/vxis/agent/skills/_payload_loader.py`

### Decision
- **Status:** `deferred`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=defer; Substantially ALREADY-HAVE: doctrine-as-data + validation field + verifier consumption + 'no finding without control' rule all exist (injection/xss/ssrf). Residual = data-fill for 11 skills + add false_positives field + rewire _doctrine_fla

---

## [8] Wire ScanPolicy chokepoints at the action boundary (enforce ceilings) !!!

- **ID:** `2026-06-15-R8-policy-chokepoint-wiring`
- **Source:** 0xSteph/pentest-ai-agents + GH05TCREW/pentestagent — pentest-ai Tier-1/2 capability gating; pentestagent tool-list curation
- **Category:** orchestration-safety
- **Priority(impact):** high
- **Overlap:** new-gap (10%)

### Description
Wire permit_strategy/persist_secret/permit_pivot into real shell/evasion/pivot/secret paths; consume ctx.policy. Curate destructive/injection tools OUT of Brain's visible set until yes/no approval flips.

**Workflow verdict:** adopt (adjusted priority 6).

### Verify (adversarial)
VERIFIED: chokepoints 0 production call sites; ctx.policy no consumer; ADR-013 ceiling model inert; ghost/layer.py:39 activate() unguarded; brain.py:376 exposes full tools unfiltered. CORRECTION: fail-closed action boundary (tool_registry.dispatch) + approve_destructive ALREADY exist; only ScanPolicy ceiling axis + tool-curation missing. 2 slices (permit_pivot blocked on H-exec).

### Affected VXIS Files
`src/vxis/agent/policy/chokepoints.py`, `src/vxis/ghost/layer.py`, `src/vxis/agent/brain.py`, `src/vxis/pipeline/scan_pipeline_v2.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: chokepoints 0 production call sites; ctx.policy no consumer; ADR-013 ceiling model inert; ghost/layer.py:39 activate() unguarded; brain.py:376 exposes full tools unfiltered. CORRECTION: fail-closed action boundary (tool_registry.d

---

## [9] PTI tenant isolation + secret encryption (ADR-013 day-one) !!!

- **ID:** `2026-06-15-R9-tenant-isolation-secret-encryption`
- **Source:** vxcontrol/pentagi + 0xSteph/pentest-ai-agents — pentagi path-containment; pentest-ai escape-on-write
- **Category:** security-multitenant
- **Priority(impact):** high
- **Overlap:** partial-overlap (50%)

### Description
Add tenant_id dimension to PTI store layout; encrypt-at-rest persisted secrets (fernet); wire the existing persist_secret chokepoint into the store write path.

**Workflow verdict:** defer (adjusted priority 4).

### Verify (adversarial)
Over-bundled: escape-on-write (Jinja autoescape) + within-dir containment (_safe_path) + persist_secret redaction + encrypt-redact policy axis ALL already exist. Genuinely missing: tenant_id store dimension + real encryption + persist_secret is unwired (0 call sites). ADR-013 deliberately ties this to multi-tenant/server milestone. Defer to that milestone.

### Affected VXIS Files
`src/vxis/pti/store.py`, `src/vxis/pti/trajectory.py`

### Decision
- **Status:** `deferred`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=defer; Over-bundled: escape-on-write (Jinja autoescape) + within-dir containment (_safe_path) + persist_secret redaction + encrypt-redact policy axis ALL already exist. Genuinely missing: tenant_id store dimension + real encryption + persist_secre

---

## [10] Enforce rate limits / total-request cap / wall-clock deadline (activate dead config) !!

- **ID:** `2026-06-15-R10-rate-limit-time-budget`
- **Source:** vxcontrol/pentagi + 0xSteph/pentest-ai-agents — pentagi cancellable task context; pentest-ai rate-limit-by-default
- **Category:** orchestration-safety
- **Priority(impact):** medium
- **Overlap:** new-gap (10%)

### Description
Enforce scope.rate_limits (max_rps/concurrent/total) + time_window (allowed_hours) in egress path; add loop-level monotonic deadline + cost-abort. Reuse existing TokenBucketRateLimiter.

**Workflow verdict:** adopt (adjusted priority 8).

### Verify (adversarial)
VERIFIED: scope.rate_limits/time_window are dead config (0 consumers); no loop deadline; latency_ms hardcoded 0. CORRECTION: Hands already does adaptive rps + 429/WAF backoff and orchestrator has a token-bucket; truly-missing = total-request cap, max_concurrent, allowed_hours window, loop deadline. The asyncio cancellation-scope half is the harder part (M+).

### Affected VXIS Files
`src/vxis/scope/runtime_gate.py`, `src/vxis/scope/enforcer.py`, `src/vxis/agent/scan_loop_run.py`, `src/vxis/interaction/hands.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: scope.rate_limits/time_window are dead config (0 consumers); no loop deadline; latency_ms hardcoded 0. CORRECTION: Hands already does adaptive rps + 429/WAF backoff and orchestrator has a token-bucket; truly-missing = total-reques

---

## [11] Real per-scan/per-agent cost ledger + budget abort !!

- **ID:** `2026-06-15-R11-per-decision-cost-ledger`
- **Source:** vxcontrol/pentagi + 0xSteph/pentest-ai-agents — pentagi cost ledger; pentest-ai ToolCall lifecycle logging
- **Category:** llm-ops
- **Priority(impact):** medium
- **Overlap:** new-gap (10%)

### Description
Feed real _estimate_usage_cost into BrainCostRouter/TrajectoryRecord (currently hard-zero); key usage per-scan/per-agent; add in-scan spend-abort guardrail.

**Workflow verdict:** adopt (adjusted priority 14).

### Verify (adversarial)
VERIFIED: cost_usd=0.0 hardcoded + len//4 tokens flow into router/trajectory; real cost only in process-global UI blob. CORRECTION: vxis does NOT use litellm at runtime (raw urllib) so no response_cost hook -- use tokens*price-table. Budget tracker already exists at growth layer; new part = in-scan abort. Skill-ledger half touches ADR-006-frozen surfaces; do cost-wiring core first. Depends on de-global (R-userglobals).

### Affected VXIS Files
`src/vxis/agent/brain_metrics.py`, `src/vxis/agent/scan_loop_v3.py`, `src/vxis/agent/routing/cost_router.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: cost_usd=0.0 hardcoded + len//4 tokens flow into router/trajectory; real cost only in process-global UI blob. CORRECTION: vxis does NOT use litellm at runtime (raw urllib) so no response_cost hook -- use tokens*price-table. Budget

---

## [12] HTTP request smuggling skill (CL.TE/TE.CL/H2 desync) !!

- **ID:** `2026-06-15-R12-http-request-smuggling`
- **Source:** usestrix/strix — strix http_request_smuggling.md (#405)
- **Category:** skill-coverage
- **Priority(impact):** medium
- **Overlap:** already_exists (85%)

### Description
REJECTED. Already-have: http_protocol_agent.py is a dedicated smuggling/desync/cache/cors agent (nuclei -tags smuggling,desync), wired into web scope. Architecturally infeasible as a skill: vxis Hands wraps httpx which normalizes CL/TE headers (raw socket = CLAUDE.md raw-httpx ban). Only residual = add a WEB-SMUGGLE scoring vector (tiny data add).

**Workflow verdict:** reject (adjusted priority 3).

### Verify (adversarial)
REJECTED. Already-have: http_protocol_agent.py is a dedicated smuggling/desync/cache/cors agent (nuclei -tags smuggling,desync), wired into web scope. Architecturally infeasible as a skill: vxis Hands wraps httpx which normalizes CL/TE headers (raw socket = CLAUDE.md raw-httpx ban). Only residual = add a WEB-SMUGGLE scoring vector (tiny data add).

### Affected VXIS Files
`(rejected) scoring/vectors.py only`

### Decision
- **Status:** `rejected`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=reject; REJECTED. Already-have: http_protocol_agent.py is a dedicated smuggling/desync/cache/cors agent (nuclei -tags smuggling,desync), wired into web scope. Architecturally infeasible as a skill: vxis Hands wraps httpx which normalizes CL/TE head

---

## [13] Fix CRLF/header-injection & XXE delivery + oracle !!

- **ID:** `2026-06-15-R13-crlf-xxe-real-oracle`
- **Source:** usestrix/strix — strix header_injection.md + ssti.md
- **Category:** skill-coverage
- **Priority(impact):** medium
- **Overlap:** already_exists (85%)

### Description
REJECTED. Host-header/cache poisoning already in http_protocol_agent/cdn_edge_agent; XML-body XXE already in fuzzing_zeroday_agent; header-injection+SSTI oracles already in verifier_tools.py:452-492; mro gadget already in payloads. The 'R2 collaborator' dependency doesn't exist. Skill-logic edits conflict with ADR-006. Only real in-scope bit = fill non-empty detect[] for crlf/xxe JSON payloads (data tweak).

**Workflow verdict:** reject (adjusted priority 4).

### Verify (adversarial)
REJECTED. Host-header/cache poisoning already in http_protocol_agent/cdn_edge_agent; XML-body XXE already in fuzzing_zeroday_agent; header-injection+SSTI oracles already in verifier_tools.py:452-492; mro gadget already in payloads. The 'R2 collaborator' dependency doesn't exist. Skill-logic edits conflict with ADR-006. Only real in-scope bit = fill non-empty detect[] for crlf/xxe JSON payloads (data tweak).

### Affected VXIS Files
`(rejected) src/vxis/data/payloads/injection.json detect[] only`

### Decision
- **Status:** `rejected`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=reject; REJECTED. Host-header/cache poisoning already in http_protocol_agent/cdn_edge_agent; XML-body XXE already in fuzzing_zeroday_agent; header-injection+SSTI oracles already in verifier_tools.py:452-492; mro gadget already in payloads. The 'R2 

---

## [14] Statistical/repeat confirmation for timing & differential signals (median-of-N) !!

- **ID:** `2026-06-15-R14-statistical-timing-confirmation`
- **Source:** 0xSteph/pentest-ai-agents + usestrix/strix — pentest-ai poc-validator FP heuristics; strix cross-channel
- **Category:** fp-verifier
- **Priority(impact):** medium
- **Overlap:** new-gap (10%)

### Description
Replace single-shot timing/diff oracles with median-of-N + jitter baseline; time-based SQLi confirmed only if >=2.5s reproduces across N probes; make repeat_count real by actual re-probing (not PoC text marker).

**Workflow verdict:** adopt (adjusted priority 12).

### Verify (adversarial)
VERIFIED: time-based SQLi fires on ONE >=2.5s observation (network-jitter FP source); repeat_count>=2 is theater (reads literal word 'twice' from PoC narrative). Real correctness/FP fix on the strongest oracles. Effort S (skill must emit structured repeat_count int).

### Affected VXIS Files
`src/vxis/agent/skills/test_injection.py`, `src/vxis/agent/skills/test_ssrf.py`, `src/vxis/agent/tools/finding_tools.py`

### Decision
- **Status:** `approved`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=adopt; VERIFIED: time-based SQLi fires on ONE >=2.5s observation (network-jitter FP source); repeat_count>=2 is theater (reads literal word 'twice' from PoC narrative). Real correctness/FP fix on the strongest oracles. Effort S (skill must emit st

---

## [15] Skill-catalog in-prompt + retrieve-then-stage top-K loading !!

- **ID:** `2026-06-15-R15-skill-rag-retrieval`
- **Source:** usestrix/strix + GH05TCREW/pentestagent — strix <available_skills>; pentestagent RAG tool-retrieval
- **Category:** llm-ops
- **Priority(impact):** medium
- **Overlap:** already_exists (95%)

### Description
ALREADY-HAVE. select_skill_cards/_score_card (skill_context.py:327-507) do top-K keyword retrieval, char-capped, recomputed each iteration, injected as non-persistent in-context block; RunSkillTool.description surfaces the full catalog; SKILL_REGISTRY is single source of truth; alias normalization absorbs improvised names. Embeddings over ~24 cards unwarranted. No gap.

**Workflow verdict:** already-have (adjusted priority 3).

### Verify (adversarial)
ALREADY-HAVE. select_skill_cards/_score_card (skill_context.py:327-507) do top-K keyword retrieval, char-capped, recomputed each iteration, injected as non-persistent in-context block; RunSkillTool.description surfaces the full catalog; SKILL_REGISTRY is single source of truth; alias normalization absorbs improvised names. Embeddings over ~24 cards unwarranted. No gap.

### Affected VXIS Files
`(already-have) src/vxis/agent/skill_context.py`

### Decision
- **Status:** `rejected`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=already-have; ALREADY-HAVE. select_skill_cards/_score_card (skill_context.py:327-507) do top-K keyword retrieval, char-capped, recomputed each iteration, injected as non-persistent in-context block; RunSkillTool.description surfaces the full catalog; SKI

---

## [16] Drop XML/delimiter envelopes around attacker-controlled evidence !!

- **ID:** `2026-06-15-R16-untrusted-content-framing`
- **Source:** usestrix/strix + 0xSteph/pentest-ai-agents — strix 25decb0 </content> injection fix; pentest-ai indirect-injection
- **Category:** llm-ops
- **Priority(impact):** medium
- **Overlap:** already_exists (95%)

### Description
ALREADY-HAVE. vxis already uses labeled-header/key=value passing everywhere (brain.py _build_smart_history, agent_graph worker prompts, compressor.as_prompt); zero content-wrapping XML envelopes; zero 'trust this delimiter' instructions. Proposed end-state already exists. Residual = one-line invariant note in CLAUDE.md/wiki to prevent regression.

**Workflow verdict:** already-have (adjusted priority 3).

### Verify (adversarial)
ALREADY-HAVE. vxis already uses labeled-header/key=value passing everywhere (brain.py _build_smart_history, agent_graph worker prompts, compressor.as_prompt); zero content-wrapping XML envelopes; zero 'trust this delimiter' instructions. Proposed end-state already exists. Residual = one-line invariant note in CLAUDE.md/wiki to prevent regression.

### Affected VXIS Files
`(already-have) CLAUDE.md invariant note only`

### Decision
- **Status:** `rejected`
- **Decision reason:** [auto from analysis 2026-06-15] verdict=already-have; ALREADY-HAVE. vxis already uses labeled-header/key=value passing everywhere (brain.py _build_smart_history, agent_graph worker prompts, compressor.as_prompt); zero content-wrapping XML envelopes; zero 'trust this delimiter' instructions. Pr

---
