# SDK Runtime Incubator

OpenAI Agents SDK worker-runtime seed. Not production-wired.

Promotion gate:
- worker-only substrate, never director runtime
- source/scope gate
- report evidence
- benchmark target
- regression tests

Install local deps only when working in this incubator:

```bash
uv pip install -r incubator/sdk_runtime/requirements.txt
```
