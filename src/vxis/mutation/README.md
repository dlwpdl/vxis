# `src/vxis/mutation/` — Attack Chain Mutation Engine (Legacy P11)

Mutation engine that takes a discovered attack chain and generates variants ("same destination, different path") to find WAF-bypassing alternatives.

Not consumed by Phase A. Deprecated along with `phases/` and slated for deletion in Task 12 unless Phase B adopts it into the Brain's toolset.
