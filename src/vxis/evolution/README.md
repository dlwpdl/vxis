# `src/vxis/evolution/` — Self-Evolving Agent Synthesis (Legacy P12)

Automatic generation of new agent capabilities when the Brain identifies a gap during a scan (e.g. "we don't have a handler for gRPC — synthesize one").

Not consumed by Phase A. Concept is interesting for Phase C (episodic memory + agent fleet expansion) but no active plans yet.
