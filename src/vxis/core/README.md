# `src/vxis/core/` — Cross-Cutting Utilities

Low-level helpers used across multiple modules. Keep small and dependency-light — anything that grows should move into its own module.

Avoid putting business logic here.
