__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
.ruff_cache/
.pytest_cache/
*.db
*.sqlite
.env
/data/
reports/

# Benchmark runs — keep score.json as evidence, skip large HTML + verbose logs
benchmarks/**/*.html
benchmarks/**/*.log

# Upstream watch — state is cached in CI, digests are committed
tools/upstream_watch/.state.json
.worktrees/
.worktrees/
tools/benchmark/.brain/
logs/

# Claude Code agent metadata (worker locks, settings)
.claude/

# VXIS ingest raw material is generated state. Commit curated digests instead.
.vxis/cache/extractions/
.vxis/signals/inbox/ingest-*.jsonl
.vxis/signals/inbox/upstream-*.md
